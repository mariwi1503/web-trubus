import React from 'react';

interface TopBannerProps {
  transparentBg?: boolean;
}

const TopBanner: React.FC<TopBannerProps> = ({ transparentBg = false }) => {
  return null;
};

export default TopBanner;
